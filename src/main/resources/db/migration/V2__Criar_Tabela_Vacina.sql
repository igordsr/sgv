CREATE TABLE Vacina(
                        id UUID PRIMARY KEY,
                        NomeVacina VARCHAR(255),
                        doseVacina VARCHAR(255),
                        numeroLote INT
);