CREATE TABLE Unidade (
                         id UUID PRIMARY KEY,
                         nome VARCHAR(255),
                         RegistroUnidade INT,
                         Situação VARCHAR(255),
                         Vacinas VARCHAR(255)
);
